%p#notice= notice

.container
  .row
    .col-md-10
      %h2 #{team.name.upcase} - ITEMS
    .col-md-2.team_money
      %h4 Balance: #{number_with_delimiter(team.money, delimiter: " ")} gp
  .row
    .col-md-12.hr
  .well.character_list
    .row.table_title
      .col-md-4
        NAME
      .col-md-4
        CATEGORY
      .col-md-2
        CASTER LVL
      .col-md-2.price_position
        PRICE (gp)
    - items.each do |item|
      .row
        .col-md-4
          = link_to item.name, team_item_path(team, item)
        .col-md-4
          = item.category
        .col-md-2
          = item.caster_level
        .col-md-2.price_position
          = number_with_delimiter(item.price, delimiter: " ")

  .row
    .col-md-12
      = link_to 'Back', team_path(team), class: 'btn btn-default'
